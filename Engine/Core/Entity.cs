using System;
using System.Collections.Generic;
using Engine.Core;
using Microsoft.Xna.Framework;

namespace Engine.Core;

/*public class Entity {
	private Dictionary<Type, IComponent> data = new();
	private Dictionary<Type, Function> funcs = new();
	
	public void addData(IComponent component){
		data[component.GetType()] = component;
	}

	public void addFunc(Function component){
		funcs[component.GetType()] = component;
		component.Register(this);

	}
	public T GetData<T>() where T:IComponent {
		return (T)data[typeof(T)];
	}
	public T GetFunc<T>() where T:Function {
		return (T)funcs[typeof(T)];
	}


	

}*/
